#ifndef GROK
#define GROK
#endif

#include "piece_chain.h"

/*
 * clearScreen Usage
 * --------------------------------
 * Clears terminal screen and resets cursor to
 * top left.
 *
 * Example:
 *	if(readyToExitProgram == 1){
 *		clearScreen();	
 *	}
 */
void clearScreen();